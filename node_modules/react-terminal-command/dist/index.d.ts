/// <reference types="react" />
interface Props {
    command: string;
    withDark?: boolean;
    color?: string;
}
declare const ReactTerminalCommand: ({ command, withDark, color }: Props) => JSX.Element;
export default ReactTerminalCommand;
